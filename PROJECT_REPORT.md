# 数字控制系统仿真项目原理说明

本项目旨在通过仿真演示基于ARX（AutoRegressive with eXogenous input）模型的数字PI控制系统。该系统模拟了微控制器（如Arduino, ESP32）中的离散控制行为，并包含了执行器饱和（Saturation）的非线性特性。

## 1. 系统模型原理 (ARX Model)

本项目的被控对象使用的是离散时间ARX模型。ARX模型的一般形式为：

$$
A(z)y(k) = B(z)u(k) + e(k)
$$

其中：
- $y(k)$ 是时刻 $k$ 的系统输出。
- $u(k)$ 是时刻 $k$ 的系统输入（控制量）。
- $e(k)$ 是白噪声干扰（在确定性仿真中通常设为0）。
- $A(z)$ 和 $B(z)$ 是关于后移算子 $z^{-1}$ 的多项式。

### 1.1 具体模型参数

在本项目中，模型参数如下：

$$
A(z) = 1 - 1.9366 z^{-1} + 1.1523 z^{-2} - 0.2144 z^{-3}
$$

$$
B(z) = -0.001961 z^{-3}
$$

注意 $B(z)$ 的形式表明系统存在固有的纯滞后。代码中的数组 `b = [0, 0, 0, -0.001961]` 对应于 $z^0, z^{-1}, z^{-2}, z^{-3}$ 的系数（或者对应于输入历史 $u(k-1)$ 到 $u(k-4)$ 的权重，具体取决于代码实现中的索引定义）。

### 1.2 差分方程推导

将 $A(z)$ 和 $B(z)$ 代入主方程，并忽略噪声 $e(k)$：

$$
(1 - 1.9366 z^{-1} + 1.1523 z^{-2} - 0.2144 z^{-3}) y(k) = -0.001961 z^{-3} u(k)
$$

展开后得到：

$$
y(k) - 1.9366 y(k-1) + 1.1523 y(k-2) - 0.2144 y(k-3) = -0.001961 u(k-3)
$$

移项得到用于迭代计算的差分方程：

$$
y(k) = 1.9366 y(k-1) - 1.1523 y(k-2) + 0.2144 y(k-3) - 0.001961 u(k-3)
$$

**注意代码实现细节**：
在代码中，变量 `u1`, `u2`, `u3`, `u4` 分别代表 $u(k-1), u(k-2), u(k-3), u(k-4)$（具体取决于更新逻辑）。
原始代码中的模型计算方式为：
`y(k) = -a(2)*y1 - a(3)*y2 - a(4)*y3 + b(1)*u1 + b(2)*u2 + b(3)*u3 + b(4)*u4`

由于 MATLAB 索引从 1 开始：
- `a(1)=1`, `a(2)=-1.9366`, `a(3)=1.1523`, `a(4)=-0.2144`。
- `b(1)=0`, `b(2)=0`, `b(3)=0`, `b(4)=-0.001961`。

代入后：
$$
y(k) = -(-1.9366)y_1 - (1.1523)y_2 - (-0.2144)y_3 + (-0.001961)u_4
$$
$$
y(k) = 1.9366 y_1 - 1.1523 y_2 + 0.2144 y_3 - 0.001961 u_4
$$

这意味着如果 `y1` 代表 $y(k-1)$，那么 `u4` 在此处实际上承担了 $u(k-4)$ 的角色（或者 $u(k-3)$ 加上额外的一个采样周期延迟，这取决于 $u$ 的更新是在 $y$ 计算之前还是之后）。在本项目中，$u$ 的历史更新是在 $y$ 计算之后进行的，因此在计算 $y(k)$ 时，`u1` 是 $u(k-1)$，`u4` 是 $u(k-4)$。

## 2. 数字PI控制器设计

本项目采用了 **增量式 PI 控制算法** (Incremental Form / Velocity Form)。

### 2.1 连续域 PI 公式

连续时间下的 PI 控制律为：

$$
u(t) = K_p e(t) + \frac{K_p}{T_i} \int_{0}^{t} e(\tau) d\tau
$$

其中：
- $K_p$：比例增益 (Proportional Gain)
- $T_i$：积分时间 (Integral Time)
- $e(t) = r(t) - y(t)$：误差信号

### 2.2 离散化推导 (Tustin变换/梯形法则)

为了将连续控制器转换为数字控制器，我们对积分项采用 Tustin 变换（双线性变换/梯形公式）。积分算子 $\frac{1}{s}$ 在离散域近似为：

$$
\frac{1}{s} \approx \frac{T_s}{2} \frac{1 + z^{-1}}{1 - z^{-1}}
$$

其中 $T_s$ 为采样周期。

将 PI 控制器的传递函数 $U(s) = K_p (1 + \frac{1}{T_i s}) E(s)$ 离散化：

$$
U(z) = K_p \left( 1 + \frac{1}{T_i} \frac{T_s}{2} \frac{1 + z^{-1}}{1 - z^{-1}} \right) E(z)
$$

整理通分：

$$
U(z) = K_p \left( \frac{T_i(1 - z^{-1}) + \frac{T_s}{2}(1 + z^{-1})}{T_i(1 - z^{-1})} \right) E(z)
$$

$$
(1 - z^{-1}) U(z) = K_p \left( (1 - z^{-1}) + \frac{T_s}{2 T_i}(1 + z^{-1}) \right) E(z)
$$

转化为时域差分方程（增量形式）：

$$
u(k) - u(k-1) = K_p [ e(k) - e(k-1) ] + \frac{K_p T_s}{2 T_i} [ e(k) + e(k-1) ]
$$

合并 $e(k)$ 和 $e(k-1)$ 的系数：

$$
u(k) = u(k-1) + \left( K_p + \frac{K_p T_s}{2 T_i} \right) e(k) + \left( -K_p + \frac{K_p T_s}{2 T_i} \right) e(k-1)
$$

### 2.3 代码中的系数

对比代码中的实现：
```python
K0 = Kp + Kp * Ts / (2 * Ti)
K1 = -Kp + Kp * Ts / (2 * Ti)
u = u1 + K0 * error + K1 * error1
```

可以看出：
- $K_0 = K_p (1 + \frac{T_s}{2 T_i})$
- $K_1 = K_p (-1 + \frac{T_s}{2 T_i})$

这与上述推导完全一致。

## 3. 执行器饱和 (Actuator Saturation)

在实际物理系统中，执行器（如电机电压、阀门开度）都有物理极限。为了模拟这一特性，系统引入了饱和环节。

数学描述为：

$$
u_{sat}(k) = \begin{cases}
U_{max} & \text{if } u(k) > U_{max} \\
U_{min} & \text{if } u(k) < U_{min} \\
u(k) & \text{otherwise}
\end{cases}
$$

**抗积分饱和 (Anti-windup)**:
由于采用了 **增量式** PI 算法，计算出的 $u(k)$ 是基于 $u(k-1)$ 累加得到的。当计算出的控制量被限幅后，代码会将限幅后的值用于更新历史变量 `u1`。
即：如果 $u(k)$ 被限制为 $U_{max}$，下一时刻的迭代基础 $u(k-1)$ 将是 $U_{max}$ 而不是未限幅的计算值。这是一种天然的抗积分饱和机制，防止了积分项在误差长期存在时无限累积（Windup）。

## 4. 总结

本项目通过差分方程精确地重现了被控对象的动态特性，并利用双线性变换法设计了数字PI控制器。增量式算法结合输出限幅，有效地模拟了嵌入式控制系统中的实际行为。
